"""Bybit connectors — Spot + Linear (perpetuals), WS + REST.

Files:
- ws_spot.py — Spot WebSocket
- ws_linear.py — Linear perpetual WebSocket (klines, tickers z funding/OI, liquidations)
- rest_spot.py — Spot REST
- rest_linear.py — Linear REST
- exchange_info.py — /v5/market/instruments-info
- parsers.py — Frame parsing
"""
